{
  "name": "mirrordock",
  "displayName": "MirrorDock",
  "description": "Mirror your Android device screen directly in VS Code",
  "version": "0.1.0",
  "publisher": "your-publisher-name",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/mirrordock"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "mirrordock-sidebar",
          "title": "MirrorDock",
          "icon": "$(device-mobile)"
        }
      ]
    },
    "views": {
      "mirrordock-sidebar": [
        {
          "type": "webview",
          "id": "mirrordock.deviceView",
          "name": "Device Screen"
        }
      ]
    },
    "commands": [
      {
        "command": "mirrordock.start",
        "title": "MirrorDock: Start",
        "icon": "$(play)"
      },
      {
        "command": "mirrordock.stop",
        "title": "MirrorDock: Stop",
        "icon": "$(stop)"
      },
      {
        "command": "mirrordock.restart",
        "title": "MirrorDock: Restart",
        "icon": "$(refresh)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "mirrordock.start",
          "when": "view == mirrordock.deviceView",
          "group": "navigation@1"
        },
        {
          "command": "mirrordock.stop",
          "when": "view == mirrordock.deviceView",
          "group": "navigation@2"
        },
        {
          "command": "mirrordock.restart",
          "when": "view == mirrordock.deviceView",
          "group": "navigation@3"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile",
    "lint": "eslint src --ext ts",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.x",
    "@types/vscode": "^1.85.0",
    "@typescript-eslint/eslint-plugin": "^6.13.0",
    "@typescript-eslint/parser": "^6.13.0",
    "eslint": "^8.54.0",
    "typescript": "^5.3.0"
  }
}